# Grid anomaly scenario

`index.ts` defines seven deterministic `ScenarioStep` records. Evidence/attention updates and capability requests are separate declarative inputs. Recovery contains three timed attention updates; the runner records each transition. Kingpin evaluates requests independently at each step's scenario time.
